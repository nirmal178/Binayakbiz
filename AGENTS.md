# Agent Instructions

- Every time you update the app, remember that you have to check the version (in `build.gradle.kts`) and increase it by one point (e.g. increase `versionCode` and bump `versionName`) before making the app publish ready.
